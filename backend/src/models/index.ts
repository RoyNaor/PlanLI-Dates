import User, { IUser } from './user.model';
import Place, { IPlace } from './place.model';
import Post, { IPost } from './post.model';
import Comment, { IComment } from './comment.model';

export {
  User,
  IUser,
  Place,
  IPlace,
  Post,
  IPost,
  Comment,
  IComment
};
